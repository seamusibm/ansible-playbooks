
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Log into OCP
      shell: cat "{{ kubeconifg }}"
      register: output
     
    - debug:
        msg: "{{ output.stdout_lines }}"
        
 #   - name: Log into OCP
 #     shell: kubectl create configmap NAME [--from-file=[key=]source] [--from-literal=key1=value1] [--dry-run=server|client|none]
